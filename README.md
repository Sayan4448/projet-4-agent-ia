# Archives historiques

Ce tag sert uniquement de point d’ancrage pour la release GitHub correspondante.

Les sources exactes de cette ancienne version n’ont pas été conservées dans
l’historique git actuel : l’artefact authentique est l’installeur `.msi`
joint à la release. Pour le code actuel, voir la branche `main`.

# Historical archives

This tag only anchors the matching GitHub release. The exact sources of this
old version were not preserved in the current git history: the attached `.msi`
installer is the authentic artifact. For current code, see the `main` branch.
